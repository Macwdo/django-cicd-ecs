[project]
name = "fastapi-cicd-ecs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.1.3",
    "django-ninja>=1.3.0",
    "ruff>=0.7.4",
]
